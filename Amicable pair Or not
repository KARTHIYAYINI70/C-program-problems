#include<stdio.h> 
int main() 
{
    int n1,n2; 
    scanf("%d %d",&n1,&n2); 
    int sum1=0,sum2=0,i; 
    for(i=1;i<n1;i++)
    {
        if(n1%i==0)
        {
            sum1=sum1+i;
        }
    }
    int j; 
    for(j=1;j<n2;j++)
    {
        if(n2%j==0)
        {
            sum2=sum2+j;
        }
    }
    if(sum1==n2&&sum2==n1)
    {
        printf("Amicable pair");
    }
    else 
    {
        printf("Not amicable pair");
    }
}
